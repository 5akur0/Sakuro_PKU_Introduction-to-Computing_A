#include <bits/stdc++.h>
using namespace std;

struct Relation {
    int x;
    int X;
};

int main() {
    int n, m;
    vector<Relation> relations(m);
    for (int i = 0; i < m; ++i) {
        cin >> relations[i].x >> relations[i].X;
        
    }
}